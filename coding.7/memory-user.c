#include <stdio.h>
#include <stdlib.h>
int main(int argc, char *argv[]){
	int bytes = arg[1];
	int ma = malloc(sizeof(bytes));

	while(true){
		for(int i=0; i < bytes; i++){
			ma[i];
		}
	}
	return 0;

}
